if ($.Redactor !== undefined) {
  $.Redactor.opts.definedLinks = [
    <% if (root = Comfy::Cms::Site.first&.pages&.root) %>
      <% root.children.published.each do |page| %>
        { "name": "<%= page.label %>", "url": "<%= page.full_path %>" },
      <% end %>
    <% end %>
  ]
}
