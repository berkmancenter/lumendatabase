<section class="notice-body">
  <% if notice.body.present? %>
    <h5 class="title"><%= notice.customizations&.fetch('body_field_label') || 'Explanation of complaint' %></h5>
    <p class="body"><pre><%= notice.redacted(:body) %></pre></p>
  <% end %>
</section>

<%= notice.customizations&.fetch('static_works_text')&.html_safe %>

<% unless notice.customizations&.fetch('hide_works') %>
  <section class="works">
    <dl class="urls">
      <dt class="label">Problematic URLs</dt>
      <dd class="field"><%= Lumen::REDACTION_MASK %></dd>
    </dl>
  </section>
<% end %>
