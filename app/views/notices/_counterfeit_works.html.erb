<section class="works">
  <ol id="works" class="copyright-claims">
    <% works.each_with_index do |work, idx| %>
      <%= content_tag_for(:li, work) do %>
        <h4>Counterfeit claim #<%= idx + 1 %></h4>
        <div class="row">
          <div class="description">
            <span class="label">Description</span> <span class="field"><%= work.description %></span>
          </div>
        </div>

        <%= render 'works_urls',
                    work: work,
                    copyrighted_title: 'Original URLs:',
                    infringing_title: 'Allegedly Infringing Counterfeit URLs:',
                    show_copyrighted: false,
                    show_infringing: true
        %>
      <% end %>
    <% end %>
  </ol>
</section>
